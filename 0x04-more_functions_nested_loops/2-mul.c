/**
 * mul- multiplies two integers
 *
 * @a: first integer to be multied
 * @b: second integer to be multiplied
 *
 * Return: the multiplication result
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
